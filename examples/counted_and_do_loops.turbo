func main() {
    int contador = 0;
    int i = 0;

    while (contador < 3) {
        print(contador);
        contador = contador + 1;
    }

    do {
        print(contador);
        contador = contador - 1;
    } while (contador > 0);

    for i = 1 to 3 {
        print(i);
    }
}

func sum_array(int arr, int size) -> int {
    int total = 0;
    int i = 0;

    while (i < size) {
        total = total + arr[i];
        i = i + 1;
    }

    return total;
}

func main() {
    print(sum_array(0, 0));
}

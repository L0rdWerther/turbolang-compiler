func classificar(int n) -> int {
    if (n > 0) {
        return 1;
    } else {
        if (n < 0) {
            return -1;
        } else {
            return 0;
        }
    }
}

func main() {
    print(classificar(10));
    print(classificar(-5));
    print(classificar(0));
}
